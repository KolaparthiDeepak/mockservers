openapi: 3.0.3
info: { title: X, version: 1.0.0 }
paths:
  /users/{id}:
    get:
      operationId: getUser
      responses:
        "200":
          description: ok
          content:
            application/json:
              examples:
                sample: { value: { id: "u1", name: "Ada" } }
  /ping:
    post:
      responses:
        "201":
          description: created
          content:
            application/json:
              example: { pong: true }
