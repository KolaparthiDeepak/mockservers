openapi: 3.0.3
info: { title: X, version: 1.0.0 }
paths:
  /bare:
    get:
      operationId: bare
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: { type: object, properties: { a: { type: string } } }
